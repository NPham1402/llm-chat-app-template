{
  "name": "chatbox-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for ChatBox AI - Context management and tool integration",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/index.js",
    "start": "node dist/index.js"
  },
  "keywords": ["mcp", "model-context-protocol", "ai", "chatbox"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
